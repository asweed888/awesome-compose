version: "3"
services:
  ${NS}_app2:
    image: alpine
    container_name: ${NS}_app2
    restart: on-failure
    environment:
      - TZ
      - SERVICE_HOST=${SERVICE_HOST}
      - SERVICE_DOMAIN=${SERVICE_DOMAIN}
      - SERVICE_URL="${SERVICE_DOMAIN}/bar/app2"
    command: /bin/sh -c "while :; do sleep 10; done"
